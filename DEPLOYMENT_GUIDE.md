# دليل النشر — من المتطلبات إلى الإنتاج

هذا الدليل يشرح خطوة بخطوة كيفية نشر نظام Campaign Site Factory بالكامل: مصنع المواقع (على
Render) وقالب موقع الجهة (على Vercel)، من حساب فارغ حتى أول جهة إعلامية منشورة فعليًا.

> **قبل أن تبدأ**: هذا الكود مفحوص صياغيًا ومُختبَر بمحاكاة قاعدة البيانات، وشُغِّل خادم المصنع
> فعليًا محليًا مع طلبات HTTP حقيقية — لكنه لم يُختبر بعد ضد Vercel أو Render حقيقيَّين. لهذا
> يخصص هذا الدليل قسمًا كاملاً (الخطوة 6) لتجربة النظام على **جهة وهمية** قبل أي بيانات حقيقية.
> هذه ليست خطوة اختيارية.

---

## 1. نظرة عامة على البنية

```
                    أنت (فريق التشغيل)
                          │
                          ▼
              ┌─────────────────────┐
              │  لوحة تحكم المصنع     │  ← Render (خدمة Node + قاعدة Postgres)
              │  (factory/)          │
              └──────────┬───────────┘
                          │ عند إنشاء جهة جديدة:
                          │ 1) ينشئ مخططًا (schema) جديدًا في نفس قاعدة بيانات Render
                          │ 2) ينشئ مشروع Vercel جديدًا وينشره
                          ▼
              ┌─────────────────────┐
              │  موقع الجهة           │  ← Vercel (مشروع مستقل لكل جهة)
              │  (tenant-template/)  │
              └──────────┬───────────┘
                          │ DATABASE_URL (نفسه لكل الجهات) + DB_SCHEMA (فريد لكل جهة)
                          ▼
              ┌─────────────────────┐
              │  خادم Postgres واحد   │  ← Render — يستضيف بيانات المصنع وكل الجهات معًا،
              │  (مخطط لكل جهة)       │     معزولة منطقيًا بمخطط Postgres مستقل لكل جهة
              └─────────────────────┘
```

**نقطتان أساسيتان لفهم بقية الدليل:**
- خادم قاعدة البيانات **واحد فقط** لكل النظام. لا تُنشئ قاعدة بيانات جديدة لكل جهة — المصنع
  يُنشئ **مخططًا (schema)** جديدًا داخل نفس القاعدة، وهذا فوري ومجاني عمليًا.
- كل جهة تحصل على **مشروع Vercel مستقل بالكامل** (نطاقه الخاص، بيئة تشغيله الخاصة)، لكنها
  تتصل بنفس خادم قاعدة البيانات مع مخططها الخاص فقط.

---

## 2. المتطلبات الأساسية

قبل البدء، تأكد من توفر:

| المتطلب | لماذا |
|---|---|
| حساب [GitHub](https://github.com) (أو أي مزوّد git آخر) | لاستضافة كود المصنع وقالب الجهة، وليربط به Render وVercel |
| حساب [Render](https://render.com) | لاستضافة المصنع وقاعدة بيانات Postgres المشتركة |
| حساب [Vercel](https://vercel.com) | لاستضافة موقع كل جهة |
| نطاق (domain) مسجَّل، ومُضاف إلى فريقكم في Vercel | لإنشاء نطاقات فرعية تلقائية لكل جهة، مثل `orgname.campaigns.example.com` |
| Node.js 18 أو أحدث مثبّت محليًا | لتوليد المفاتيح، وللاختبار المحلي الاختياري |
| أداة سطر الأوامر `psql` (عميل PostgreSQL) | لتشغيل ملف الهجرة الأول على قاعدة بيانات المصنع |
| إلمام أساسي بسطر الأوامر (Terminal) | لتنفيذ أوامر هذا الدليل |

لا تحتاج أي حساب أو خدمة أخرى غير Render وVercel.

---

## 3. تجهيز المستودع (Repository)

يحتاج المشروع مستودعَي git منفصلَين (كل واحد يُنشر على منصة مختلفة):

1. **مستودع قالب الجهة** — يحتوي على مجلد `tenant-template/` فقط (أو المجلد بأكمله إن كنتم
   تفضّلون جعل `tenant-template/` هو جذر المستودع). هذا هو المستودع الذي سيُنشئ منه المصنع
   مشروع Vercel جديدًا لكل جهة.
2. **مستودع المصنع** — يحتوي على مجلد `factory/` (أو المجلد بأكمله كمستودع جذر أيضًا، وتوجيه
   Render لبناء `factory/` تحديدًا عبر إعداد "Root Directory" في الخدمة).

```bash
# مثال: رفع قالب الجهة كمستودع منفصل
cd tenant-template
git init
git add .
git commit -m "النسخة الأولى من قالب موقع الجهة"
git remote add origin https://github.com/your-org/campaign-site-template.git
git push -u origin main

# ثم بنفس الطريقة لمجلد factory/ في مستودع آخر
cd ../factory
git init
git add .
git commit -m "النسخة الأولى من مصنع المواقع"
git remote add origin https://github.com/your-org/campaign-site-factory.git
git push -u origin main
```

احتفظ باسم المستودع الأول (`your-org/campaign-site-template` في المثال) — ستحتاجه لاحقًا في
متغير `TENANT_GIT_REPO`.

---

## 4. نشر المصنع على Render

### 4.1 النشر عبر Blueprint

1. من لوحة تحكم Render: **New** ← **Blueprint**.
2. اختر مستودع المصنع الذي رفعته في الخطوة السابقة.
3. سيقرأ Render ملف `factory/render.yaml` تلقائيًا وسيعرض عليك موردَين للموافقة عليهما:
   - قاعدة بيانات باسم `campaign-factory-db`
   - خدمة ويب باسم `campaign-site-factory`
4. اضغط **Apply** — سينشئ Render المورَدين معًا، ويربط `DATABASE_URL` بينهما تلقائيًا.
5. انتظر حتى تكتمل قاعدة البيانات (عادة دقيقة إلى دقيقتين) ثم بناء الخدمة (بضع دقائق إضافية).

### 4.2 تشغيل مخطط قاعدة بيانات المصنع

بعد اكتمال قاعدة البيانات:

1. من لوحة Render، افتح `campaign-factory-db` ← تبويب **Connect** ← انسخ **External Database
   URL**.
2. من جهازك (وليس من داخل Render):
   ```bash
   psql "<External Database URL>" -f factory/migrations/001_factory_schema.sql
   ```
3. تحقّق من نجاح العملية: `psql "<External Database URL>" -c "\dt"` يجب أن يعرض جداول مثل
   `tenants`, `provisioning_jobs`, `super_admins`.

### 4.3 توليد مفتاح التشفير

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
احتفظ بالناتج (نص hex من 64 حرفًا) — ستحتاجه في الخطوة التالية. **لا تشاركه في أي مكان آخر غير
لوحة متغيرات البيئة في Render.**

### 4.4 ضبط متغيرات البيئة المتبقية

من خدمة `campaign-site-factory` في لوحة Render ← تبويب **Environment**، أضف:

| المتغير | القيمة |
|---|---|
| `FACTORY_ENCRYPTION_KEY` | الناتج من الخطوة 4.3 |
| `FACTORY_BOOTSTRAP_SECRET` | أي نص عشوائي طويل تختاره الآن (مثال: `openssl rand -hex 24`) |
| `FACTORY_REQUIRE_2FA` | `true` |
| `TENANT_BASE_DOMAIN` | نطاقكم الفرعي الأساسي، مثل `campaigns.example.com` |
| `TENANT_GIT_REPO` | اسم مستودع قالب الجهة، مثل `your-org/campaign-site-template` |

اترك `VERCEL_TOKEN` و`VERCEL_TEAM_ID` و`TENANT_GIT_REPO_ID` فارغة الآن — ستُضبط في الخطوة 5.

بعد الحفظ، ستُعيد Render نشر الخدمة تلقائيًا. افتح رابط الخدمة (يظهر أعلى صفحتها في لوحة Render)
— يجب أن تظهر شاشة تسجيل الدخول للوحة المصنع.

---

## 5. الحصول على مفاتيح Vercel وربطها

### 5.1 توكن Vercel

1. [لوحة Vercel](https://vercel.com/account/tokens) ← **Create Token**.
2. أعطه اسمًا واضحًا (مثل `campaign-factory-provisioning`)، وحدد النطاق (Scope) على فريقكم إن
   كنتم تعملون ضمن فريق.
3. انسخ التوكن فور ظهوره (لن يُعرض مرة أخرى).

### 5.2 معرّف الفريق (Team ID)

إن كان التوكن أعلاه مرتبطًا بفريق (لا حساب شخصي): من إعدادات الفريق في Vercel ← **General** ←
انسخ **Team ID** (يبدأ عادة بـ `team_`).

### 5.3 معرّف مستودع قالب الجهة (Repo ID)

هذا الحقل الأكثر التباسًا؛ إليك أسهل طريقة عملية:

1. من لوحة Vercel، أنشئ مشروعًا يدويًا واحدًا مرتبطًا بمستودع قالب الجهة (**Add New** ←
   **Project** ← اختر المستودع ← Deploy). هذا مشروع تجريبي فقط، ستحذفه بعد الحصول على الرقم.
2. بعد نجاح النشر، شغّل:
   ```bash
   curl -H "Authorization: Bearer <VERCEL_TOKEN>" \
     "https://api.vercel.com/v9/projects/<اسم-المشروع-التجريبي>"
   ```
3. ابحث في الناتج عن حقل `link.repoId` (أو `repoId` حسب إصدار الـ API الحالي) — هذا هو الرقم
   المطلوب.
4. احذف المشروع التجريبي (**Settings** ← **Advanced** ← **Delete Project**) — لن تحتاجه بعد
   الآن؛ المصنع سينشئ مشاريع الجهات تلقائيًا لاحقًا.

> ⚠️ نقاط نهاية Vercel API (خصوصًا إصدار `/v13/deployments` المستخدم في
> `factory/lib/vercelClient.js`) من أكثر الأجزاء تغيّرًا تاريخيًا في توثيق Vercel. إن واجهت خطأ
> عند إنشاء أول جهة، راجع
> [توثيق Vercel الحالي](https://vercel.com/docs/rest-api/reference/endpoints/deployments)
> أولًا للتأكد من تطابق شكل الطلب.

### 5.4 إضافة النطاق الأساسي إلى فريق Vercel

من إعدادات الفريق ← **Domains** ← أضف نطاقكم الأساسي (مثل `example.com` أو
`campaigns.example.com`) واتبع تعليمات DNS المعروضة (عادة إضافة سجل CNAME أو NS عند مزوّد
النطاق لديكم). هذا ضروري حتى يستطيع المصنع ربط نطاقات فرعية مثل `orgname.campaigns.example.com`
تلقائيًا لكل جهة.

### 5.5 حفظ المفاتيح في المصنع

عد إلى متغيرات البيئة في خدمة `campaign-site-factory` على Render وأضف:

| المتغير | القيمة |
|---|---|
| `VERCEL_TOKEN` | التوكن من 5.1 |
| `VERCEL_TEAM_ID` | من 5.2 (اتركه فارغًا إن كنتم تستخدمون حسابًا شخصيًا لا فريقًا) |
| `TENANT_GIT_REPO_ID` | من 5.3 |

---

## 6. الإعداد الأول للوحة التحكم

### 6.1 إنشاء أول مشرف عام

```bash
curl -X POST https://<اسم-خدمتكم>.onrender.com/api/factory/bootstrap \
  -H "Content-Type: application/json" \
  -d '{
    "bootstrapSecret": "<FACTORY_BOOTSTRAP_SECRET من 4.4>",
    "name": "اسمك الكامل",
    "username": "اسم المستخدم الذي تريده",
    "password": "كلمة مرور قوية لا تقل عن 12 حرفًا"
  }'
```
استجابة ناجحة تعني إنشاء الحساب. **هذا المسار يعمل مرة واحدة فقط** — بعد وجود مشرف واحد، سيرفض
أي محاولة لاحقة.

### 6.2 تسجيل الدخول وتفعيل التحقق بخطوتين

1. افتح رابط خدمة المصنع في المتصفح.
2. سجّل الدخول باسم المستخدم وكلمة المرور من 6.1.
3. بما أن `FACTORY_REQUIRE_2FA=true`، ستظهر شاشة مسح رمز QR — امسحه بتطبيق مصادقة (Google
   Authenticator أو Authy أو ما شابه)، ثم أدخل الرمز المكوّن من 6 أرقام لتأكيد التفعيل.
4. بعد التفعيل، ستدخل مباشرة إلى لوحة التحكم الرئيسية.

---

## 7. تجربة كاملة على جهة وهمية (خطوة إلزامية)

**لا تنشئ جهة حقيقية قبل إتمام هذه الخطوة.** الهدف التأكد أن كل مكوّن يعمل فعليًا في بيئة
الإنتاج، وليس فقط في الاختبارات المحلية.

1. من لوحة التحكم، اضغط **"+ إنشاء جهة جديدة"**.
2. أكمل المعالج ببيانات وهمية واضحة (مثال: اسم الجهة "جهة تجريبية — احذفني"، معرّف فريد
   `test-delete-me`).
3. راقب شاشة "خط الإنتاج" التي تظهر تلقائيًا بعد الإرسال — يجب أن تتحول كل خطوة من الخطوات
   العشر إلى ✓ خضراء بالتتابع خلال دقيقة إلى دقيقتين تقريبًا.
4. إن توقفت خطوة بعلامة ✕ حمراء، اقرأ رسالة الخطأ الظاهرة تحتها مباشرة — راجع قسم **استكشاف
   الأخطاء** أدناه للأخطاء الشائعة المرتبطة بكل خطوة.
5. بعد نجاح كل الخطوات:
   - افتح رابط الموقع الناتج، وتأكد من ظهور الصفحة الرئيسية بشكل صحيح.
   - اضغط "كلمة مرور الأدمن" من قائمة الجهات، وسجّل الدخول بها على `/admin` في موقع الجهة.
   - جرّب تعديل الهوية البصرية (زر "تعديل الهوية") — غيّر لونًا وارفع شعارًا تجريبيًا، واحفظ.
   - أعد فتح موقع الجهة وتأكد أن التغيير ظهر.
   - جرّب أزرار المشاركة (X/واتساب/فيسبوك) من الصفحة الرئيسية للتأكد من عملها.
6. احذف الجهة التجريبية (زر "حذف" في قائمة الجهات، مع كتابة معرّفها للتأكيد) وتأكد أن مشروع
   Vercel الخاص بها اختفى من لوحة Vercel أيضًا (المصنع يحذفه تلقائيًا).

إن نجحت كل الخطوات أعلاه، النظام جاهز لإنشاء جهات حقيقية.

---

## 8. إنشاء أول جهة حقيقية

نفس خطوات القسم 7، ببيانات الجهة الفعلية. بعد الإنشاء:

- شارك رابط الموقع مع الجهة.
- شارك كلمة مرور الأدمن معها عبر قناة آمنة (وليس بريدًا إلكترونيًا عاديًا أو رسالة غير مشفّرة) —
  ستُطالَب بتغييرها عند أول دخول تلقائيًا.
- سجّل بيانات الجهة (الاسم، المعرّف، تاريخ الإنشاء) في أي نظام تتبّع داخلي لديكم.

---

## 9. العمليات التشغيلية بعد الإطلاق

| المهمة | كيف |
|---|---|
| تحديث القالب لجهة واحدة | زر "تحديث القالب" على بطاقة الجهة — يعيد نشر أحدث نسخة من مستودع القالب |
| تحديث القالب لكل الجهات دفعة واحدة | `POST /api/factory/template/bulk-update` (لا يوجد زر في الواجهة حاليًا لهذا — استدعه مباشرة عبر `curl` مع رمز الجلسة) |
| تعديل هوية جهة (اسم، ألوان، شعار، منصات) | زر "تعديل الهوية" على بطاقة الجهة |
| إعادة تعيين كلمة مرور أدمن جهة | `POST /api/factory/tenants/:id/reset-admin-password` ثم `GET .../admin-password` لعرضها مرة واحدة |
| تعليق جهة مؤقتًا | زر "تعليق" — يوقف الوصول للموقع دون حذف البيانات |
| مراجعة من فعل ماذا | تبويب "سجل التدقيق" في اللوحة |
| تدوير `FACTORY_ENCRYPTION_KEY` | ولّد مفتاحًا جديدًا، ثم أعد تشفير كل صف في جدول `secrets_vault` يدويًا قبل استبدال المتغير (وإلا ستفقد الوصول للأسرار المخزَّنة) |
| النسخ الاحتياطي لقاعدة البيانات | فعّل النسخ الاحتياطي التلقائي من إعدادات قاعدة `campaign-factory-db` في Render (يتطلب خطة مدفوعة أعلى من `starter` في بعض المناطق — تحقق من الخطط الحالية) |

---

## 10. استكشاف الأخطاء الشائعة

| العرض | السبب المحتمل | الحل |
|---|---|---|
| خطوة "إنشاء مشروع Vercel" تفشل بخطأ 401/403 | `VERCEL_TOKEN` غير صحيح أو منتهي الصلاحية | أعد توليد التوكن من 5.1 وحدّثه في متغيرات بيئة Render |
| خطوة "النشر" تفشل أو تتوقف طويلًا | شكل `gitSource.repoId` غير مطابق لما تتوقعه نقطة نهاية Vercel الحالية | راجع الملاحظة في §5.3، وتحقق من إصدار `/v13/deployments` في `lib/vercelClient.js` مقابل [توثيق Vercel](https://vercel.com/docs/rest-api/reference/endpoints/deployments) |
| خطوة "ربط النطاق" تفشل | النطاق الأساسي غير مُضاف لفريق Vercel، أو DNS لم يُفعَّل بعد | أكمل خطوات §5.4، وانتظر انتشار DNS (قد يستغرق حتى 24 ساعة أحيانًا) |
| خطوة "الفحص الصحي" تفشل رغم نجاح النشر | الموقع يستغرق وقتًا أطول من المتوقع ليصبح جاهزًا (Cold start) | أعد المحاولة من لوحة التحكم بعد دقيقة؛ إن استمر الفشل، افتح رابط الموقع يدويًا وتحقق من الخطأ الظاهر |
| تسجيل الدخول للوحة المصنع يفشل بلا سبب واضح | `DATABASE_URL` غير مضبوط، أو مخطط `factory/migrations/001_factory_schema.sql` لم يُشغَّل بعد | راجع سجلات الخدمة (Logs) في Render، وتأكد من تنفيذ §4.2 |
| "طلبات كثيرة جدًا" عند تسجيل الدخول | آلية الحماية من محاولات الدخول المتكررة (IP أو حساب) نشطة | انتظر 15 دقيقة، أو راجع الكود في `factory/api/[...path].js` إن كان هذا يحدث بشكل متكرر غير متوقع |
| أداء بطيء مع نمو عدد الجهات | كل الجهات تتشارك حد `max_connections` في Postgres | ارفع خطة قاعدة البيانات في Render، أو أضف PgBouncer أمامها |

---

## 11. قائمة تحقق نهائية قبل الاعتماد الكامل على الإنتاج

- [ ] تمت تجربة إنشاء جهة كاملة وحذفها بنجاح (القسم 7)
- [ ] تسجيل الدخول للوحة المصنع يتطلب فعليًا رمز التحقق بخطوتين
- [ ] `FACTORY_BOOTSTRAP_SECRET` لم يعد صالحًا لإنشاء مشرف إضافي (تحقق: أعد استدعاء `/api/factory/bootstrap` ويجب أن يرفض)
- [ ] النسخ الاحتياطي التلقائي لقاعدة البيانات مفعّل
- [ ] كل من قام بلصق مفاتيح حقيقية في أي محادثة أو مستند غير آمن أعاد توليدها
- [ ] سجل التدقيق يعمل ويسجّل الإجراءات فعليًا (تحقق بعد أي إجراء تجريبي)
- [ ] فريق التشغيل يعرف كيف يصل لسجلات الخدمة (Logs) في Render عند حدوث مشكلة
